<h1>新規追加画面</h1>
<%= form_with model: @task, local: true do |f| %>
<% if @task.errors.any? %>
  <%= @task.errors.messages[:title][0] %>
<% end %>
  <p>
    <%= f.label :title %> <br>
    <%= f.text_area :title, placeholder: "予定を追加してください" %>
  </p>
  <p>
    <%= f.submit "送信" %>
  </p>
<% end %>